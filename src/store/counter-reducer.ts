const initialState = {
    startValue: 0,
    maxValue: 5,
    count:1,
    startError: false,
    maxError: false,
    isSet: true
}

export const counterReducer = (state = initialState ) => {

    return state;

}