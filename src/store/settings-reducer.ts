const initialState = {
    startValue: 0,
    maxValue: 5,
    startError: false,
    maxError: false,
    isEdited: false
}

export const settingsReducer = (state = initialState,) => {
    return state;
}